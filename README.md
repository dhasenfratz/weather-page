# Weather Page

A minimal static page that displays a daily generated weather image.  
The image is created externally, stored in Firebase, and retrieved by this page.  
This repository hosts the page via GitHub Pages and acts as the landing page for **dhasenfratz.com**.
